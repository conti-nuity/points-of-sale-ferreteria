import { keyframes } from "styled-components";
import { fadeIn, fadeInUp, fadeInDown, zoomIn } from "react-animations";

export const fadeInAnimation = keyframes`${fadeIn}`;
export const fadeInUpAnimation = keyframes`${fadeInUp}`;
export const fadeInDownAnimation = keyframes`${fadeInDown}`;
export const zoomInAnimation = keyframes`${zoomIn}`;
